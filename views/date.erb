<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Today</h1>
    <% require 'date'
      day = DateTime.now(%A)
      month = DateTime.now(%B)
      now = DateTime.now %>
    <p><%= "The date is #{day}, #{month} #{now.day}, #{now.year}."  %></p>
  </body>
</html>
